import React from 'react'

const ProfilePhoto = (props) => {
    return (
        <div className = "image">
            {props.children}
            
        </div>
    )
}

export default ProfilePhoto
